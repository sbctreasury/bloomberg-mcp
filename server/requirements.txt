# Core MCP framework
fastmcp>=2.0.0
pydantic>=2.0.0

# Bloomberg data access (Tier 1 & 2 — install what you have)
# If neither is available, the server falls back to bqnt-3 subprocess
xbbg>=0.11.0
polars-bloomberg>=0.5.0
blpapi>=3.24.0
polars>=1.0.0

# Charting
matplotlib>=3.8.0
altair>=5.0.0

# System utilities
psutil>=5.9.0
